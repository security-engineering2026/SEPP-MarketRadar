name: Source Scale Fast Lane
# qualification trigger: source-scale verification lane

on:
  push:
    branches:
      - 'qualification/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: source-scale-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-scale:
    name: LIVE_SOURCE_SCALE diagnostic
    runs-on: windows-latest
    timeout-minutes: 20
    env:
      PYTHONUNBUFFERED: '1'
      MR_SOURCE_VERIFY_WORKERS: '48'
      MR_SOURCE_VERIFY_TIMEOUT: '10'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements-dev.txt
      - name: Install dependencies
        shell: pwsh
        run: python -m pip install -r requirements-dev.txt
      - name: Compile
        shell: pwsh
        run: python -m compileall -q marketradar tools
      - name: Run source-scale gate
        shell: pwsh
        run: python -B tools/full_qualification.py --gate LIVE_SOURCE_SCALE_500
      - name: Run live acquisition diversity gate
        shell: pwsh
        timeout-minutes: 12
        run: python -B -c "from tools.full_qualification import live_acquisition_sample_gate; import json; r=live_acquisition_sample_gate(30); print(json.dumps(r, ensure_ascii=False, indent=2)); raise SystemExit(0 if r['status']=='PASS' else 1)"
